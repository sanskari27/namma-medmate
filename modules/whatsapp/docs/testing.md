# Testing

- Unit: Vitest on `whatsapp-api`, `whatsapp-ui`, and `db-services` WhatsApp repositories. Coverage 100%.
- API e2e: Playwright `request` happy-path + failure-cases.
- UI e2e: e2e-kit screens for inbox, mandatory banner, and share button (smoke, happy-path, failure-cases, a11y, visual).
