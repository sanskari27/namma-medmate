# Local development — copy to .env (gitignored). Used by compose.yaml substitution only.
# DATABASE_URL for server is hardcoded to local postgres in compose.yaml (never RDS).

POSTGRES_PASSWORD=postgres
JWT_SECRET=change-me-local-dev-only-use-openssl-rand-hex-64
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=
RESEND_FROM="Namma MedMate <noreply@nammamedmate.com>"
META_WHATSAPP_TOKEN=
META_WHATSAPP_PHONE_NUMBER_ID=
META_WHATSAPP_WABA_ID=
META_WHATSAPP_DISPLAY_NUMBER=
