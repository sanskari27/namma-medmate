---
id: M2-S04
epic: M2
title: Default branch and branch master
phase: 1
priority: P0
apps: [server, dispensary, admin]
personas: [OWNER, MASTER]
depends_on: [M2-S02]
blocked_by: []
sources:
  - docs/product/product-compiled.md#module-2-organization--tenant--branch-structure
  - docs/product/m2-tenancy.md
---

# M2-S04 — Default branch and branch master

## User story

As **OWNER or MASTER**, I want to **create the default branch from approved KYC and maintain complete branch records** so that **onboard verified pharmacy tenants, administer lifecycle and plans, and operate isolated branches.**

## Scope

### In

- KYC approval creates one editable default branch from the KYC address.
- Branch code is unique and generated by the system.
- Branch records include contact, address, state, city, pincode, drug license, conditional GST, operating hours, Retail/Kiosk classification, status, opening date, default flag, and linked-warehouse flag.
- Kiosk classification does not itself authorize a self-order workflow.
- Exactly one active default branch exists per tenant.
- Pricing and tax settings may differ by branch.
- Copying settings from another branch creates a point-in-time snapshot; it is never a live link.

### Out

- Behavior explicitly deferred by the product source or a linked decision.
- Adjacent module behavior not named in this story.

## Acceptance criteria

### M2-S04-AC01 — KYC approval creates one editable default branch from the KYC address

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | KYC approval creates one editable default branch from the KYC address. |

### M2-S04-AC02 — Branch code is unique and generated by the system

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | Branch code is unique and generated by the system. |

### M2-S04-AC03 — Branch records include contact, address, state, city, pincode, drug license, conditional

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | Branch records include contact, address, state, city, pincode, drug license, conditional GST, operating hours, Retail/Kiosk classification, status, opening date, default flag, and linked-warehouse flag. |

### M2-S04-AC04 — Kiosk classification does not itself authorize a self-order workflow

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | Kiosk classification does not itself authorize a self-order workflow. |

### M2-S04-AC05 — Exactly one active default branch exists per tenant

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | Exactly one active default branch exists per tenant. |

### M2-S04-AC06 — Pricing and tax settings may differ by branch

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | Pricing and tax settings may differ by branch. |

### M2-S04-AC07 — Copying settings from another branch creates a point-in-time snapshot

| Given | When | Then |
|---|---|---|
| A request or event initiated by OWNER / MASTER with the story-required role, completed dependencies, and valid platform or tenant/branch context as applicable | They create the default branch from approved KYC and maintain complete branch records | Copying settings from another branch creates a point-in-time snapshot; it is never a live link. |

### M2-S04-AC08 — Isolation and failure safety

| Given | When | Then |
|---|---|---|
| Missing permission, invalid tenant/branch scope, stale state, duplicate request, or invalid input | The create the default branch from approved KYC and maintain complete branch records operation is attempted | Missing drug license, invalid hours, duplicate code/default, or cross-tenant mutation fails. No unauthorized data is disclosed and no partial write remains |

## Implementation contract

### Server

- Branch CRUD uses /api/v1/branches and enforces tenant ownership.
- Follow `feature → application → persistence + domain`; controllers never access repositories.
- Pharmacy-owned queries require `tenant_id`; branch-owned queries also require `branch_id`.
- Validate authorization, idempotency, transaction boundaries, concurrency, and immutable audit facts at the server.

### Dispensary / Admin

- Target apps: **server, dispensary, admin**.
- Dispensary provides branch setup; admin has authorized read/support visibility.
- Handle loading, empty, validation, denied, conflict, failure, and success states with labels, keyboard access, visible focus, and focus restoration.
- UI guards improve UX only; the server remains the authorization boundary.

## Data and state

- Use UUID identifiers, UTC persistence, IST display, and INR minor units where money applies.
- Add schema only through a new Flyway migration; never edit an existing migration.
- Preserve historical transaction snapshots when referenced master data later changes.

## Required tests

- Unit tests for each business branch and validator.
- Spring integration tests for persistence, authorization, transaction rollback, tenant isolation, and branch isolation where applicable.
- Component/integration tests in every targeted React app.
- End-to-end happy path and at least one failure path for the complete cross-app workflow.
- Regression tests for every bug found while implementing this story.

## Definition of done

- [ ] Every acceptance criterion has automated evidence.
- [ ] Every dependency is `done` and linked decisions are closed.
- [ ] Tests were observed failing before runtime implementation and now pass.
- [ ] Target-specific format, lint, test, build, and compose gates pass.
- [ ] The independent story verifier returns `PASS`.
- [ ] The implementation tracker contains evidence and is the only changed status source.
