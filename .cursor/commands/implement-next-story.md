---
description: Implement the first dependency-ready requirement story
---

Use the `implement-next-story` skill. Select from the tracker, then follow
`implement-story` **in this agent** (`spring-story` and/or `react-story` from
`apps`). Do not spawn `story-implementer`. Obtain one independent
`story-verifier` pass and stop after that story reaches `done` or a documented
blocker.
