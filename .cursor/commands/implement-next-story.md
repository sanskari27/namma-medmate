---
description: Implement the first dependency-ready requirement story
---

Use the `implement-next-story` skill. Select from the tracker, then follow
`implement-story`, which dispatches `spring-story` and/or `react-story` from
the story `apps` list. Obtain independent verification and stop after that
story reaches `done` or a documented blocker.
