# Code review

Usage: `/code-review` (current diff)

Structural pass on the working tree / PR.

Skills `code-reviewer` + `clean-code` (and Ponytail). BLOCK/WARN. Does not replace `/review-module` (contract + security + tests). BLOCK a shadcn tree copied into a module or app — primitives belong in `libs/shared-ui`.
