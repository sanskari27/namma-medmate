# Env doctor

Usage: `/env-doctor` (optional failing command or module)

Trace Zod env, `.env.example`, `local-dev`, and SSM path failures.

Follow skill `env-doctor`. Never guess or print secrets. Copy `modules/auth/api/src/config/env.ts` for new APIs.
