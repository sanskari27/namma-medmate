---
description: Implement one named requirement story
argument-hint: M1-S01
---

Use the `implement-story` skill for `$ARGUMENTS` in this agent. Apply
`spring-story` and/or `react-story` from the story `apps` list. Do not spawn
`story-implementer`. Do not substitute another story and do not proceed if the
named story is blocked, deferred, already in progress elsewhere, or has
incomplete dependencies.
