---
description: Implement one named requirement story
argument-hint: M1-S01
---

Use the `implement-story` skill for `$ARGUMENTS`. Dispatch `spring-story`
and/or `react-story` from the story `apps` list. Do not substitute another
story and do not proceed if the named story is blocked, deferred, already in
progress elsewhere, or has incomplete dependencies.
