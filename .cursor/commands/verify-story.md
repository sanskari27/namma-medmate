---
description: Independently verify one implemented story
argument-hint: M1-S01
---

Use the `verify-story` skill for `$ARGUMENTS`. Dispatch `verify-spring`
and/or `verify-react` from the story `apps` list. Verification is
evidence-based and must not repair implementation while evaluating it.
Do not re-run listed full gates; cite the implementer's command output.
