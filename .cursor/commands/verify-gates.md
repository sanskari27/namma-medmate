# Verify gates

Usage: `/verify-gates` (optional Nx project names)

Run format, lint, typecheck, 100% unit coverage, and Playwright on touched projects.

Follow skill `verify-gates`. On red, skill `debug-quality-gate`. Never mark the tracker `done` while any step is red.
