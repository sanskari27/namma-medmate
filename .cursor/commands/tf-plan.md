# Terraform plan

Usage: `/tf-plan` (optional env staging|prod)

Follow skill `terraform-change`. Remote S3 state only. Never commit state, locks, or `.terraform/`.

Document blast radius. Do not apply production unless the user asks.
